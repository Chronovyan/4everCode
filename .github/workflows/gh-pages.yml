name: Deploy MkDocs to GitHub Pages

# Run on push to main branch or manually
on:
  push:
    branches: [main, gh-pages]  # Trigger on both main and gh-pages
  workflow_dispatch:
  schedule:
    # Run every Sunday to catch dependency updates
    - cron: '0 0 * * 0'

# Set permissions for the workflow
permissions:
  contents: write
  pages: write
  id-token: write

# Allow concurrent runs for faster feedback
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main  # Always build from main branch
          fetch-depth: 0  # Get full git history for accurate revision dates
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r docs/requirements-dev.txt --no-cache-dir
      
      - name: Build
        run: |
          mkdocs build --strict --verbose
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'site'  # Changed from docs/site to site
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
